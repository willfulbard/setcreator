// Include middleware for routes here

module.exports = function (app, express) {
  // Setup routes to api stuff here, like:
  // app.get('/api/links/', linksController.allLinks);
  // app.post('/api/links/', linksController.newLink);

};
